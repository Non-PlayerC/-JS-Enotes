// i = i ? i < 0 ? Math.max(0, len + i) : i : 0;
// 判断 i 是不是真值；如果是，判断是否小于0；
// 如果小于0，取 0 和 len+i 中较大的；如果大于0，将 i 赋值给 i；
// 如果 i 是假值，将 0 赋值给 i

//! 忍者之道

// if (null) {
//     console.log("test")
// } else {
//     console.log("error");
// }
